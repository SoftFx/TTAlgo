syntax = "proto3";

package ticktrader.algo.domain;

option csharp_namespace = "TickTrader.Algo.Domain";

message Update {
    enum Action {
        Added = 0;
        Updated = 1;
        Removed = 2;
    }
}

message UrlInfo {
    string address = 1;
    int32 port = 2;
}