<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:TickTrader.BotTerminal">

    <ControlTemplate x:Key="ProgressViewTemplate.Classic" TargetType="UserControl">
        <Grid Visibility="{TemplateBinding Visibility}" Margin="{TemplateBinding Padding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ProgressBar Grid.Row="0" Height="4" Margin="5"
                                 Minimum="{Binding ProgressMin}" Maximum="{Binding ProgressMax}"
                                 Value="{Binding Progress}"/>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,1,0,1" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ControlTemplate>

    <Style x:Key="ProgressViewStyle.Classic" TargetType="UserControl">
        <Setter Property="Template" Value="{StaticResource ProgressViewTemplate.Classic}"/>
        <Setter Property="Padding" Value="4,3,4,3" />
    </Style>

</ResourceDictionary>