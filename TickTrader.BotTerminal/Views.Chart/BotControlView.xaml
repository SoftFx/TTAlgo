<UserControl x:Class="TickTrader.BotTerminal.BotControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:l="clr-namespace:TickTrader.BotTerminal"
             xmlns:cal="http://www.caliburnproject.org"
             Height="24"
             Foreground="{DynamicResource Shell_PrimaryForeground}"
             mc:Ignorable="d">

    <UserControl.Resources>

        <ContextMenu x:Key="ContextMenu">
            <MenuItem Header="View state" cal:Message.Attach="OpenState" />
        </ContextMenu>

        

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

    </UserControl.Resources>

    <Border CornerRadius="3" Background="{DynamicResource Chart_BotControl_Background}"
            ContextMenu="{StaticResource ContextMenu}">
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="statusCircle" Width="5" Height="5" Margin="5 0 5 0"/>
            <TextBlock Text="{Binding Model.Name}" Width="180"/>
            <l:FlatButton x:Name="StartStop" Style="{DynamicResource BotControlStartStopButtonStyle}" IsEnabled="{Binding CanStartStop}"/>
            <l:FlatButton x:Name="Close" Style="{StaticResource BotControlCloseButtonStyle}" IsEnabled="{Binding CanBeClosed}"/>
        </StackPanel>
    </Border>
    
</UserControl>
