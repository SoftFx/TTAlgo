<Window x:Class="TickTrader.BotTerminal.BotStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:TickTrader.BotTerminal"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        Style="{StaticResource Dialog_Style}"
        Title="{Binding DisplayName}" Height="300" Width="300">

    <Window.Resources>

        <Style x:Key="BotNameLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Shell_PrimaryForeground}"/>
        </Style>

        <Style x:Key="StatusLabelStyle" TargetType="TextBlock">
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Foreground" Value="{DynamicResource Shell_SecondaryForeground}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Starting...">
                    <Setter Property="Foreground" Value="Yellow"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Stopping...">
                    <Setter Property="Foreground" Value="Yellow"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Running">
                    <Setter Property="Foreground" Value="LightGreen"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StartStopButtonStyle" TargetType="l:FlatButton" BasedOn="{StaticResource AutoColorFlatButton_Style}">
            <Setter Property="Background" Value="{DynamicResource Chart_BotControl_StartBackground}"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Path Width="14" Height="14" Stretch="Uniform" StrokeThickness="0" SnapsToDevicePixels="True"
                          Data="{StaticResource RightArrow}"
                          Fill="{DynamicResource Shell_PrimaryForeground}"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsStarted}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Chart_BotControl_StopBackground}"/>
                    <Setter Property="Content">
                        <Setter.Value>
                            <Path Width="14" Height="14" Stretch="Uniform" StrokeThickness="0" SnapsToDevicePixels="True"
                                  Data="{StaticResource Square}"
                                  Fill="{DynamicResource Shell_PrimaryForeground}"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource Dialog_ControlBackground}" CornerRadius="4" Margin="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <l:FlatButton x:Name="StartStop" Style="{StaticResource StartStopButtonStyle}" IsEnabled="{Binding CanStartStop}" Grid.RowSpan="2"/>
                <TextBlock Style="{StaticResource BotNameLabelStyle}" Text="{Binding BotName}" Grid.Column="1" Grid.Row="0" />
                <TextBlock Style="{StaticResource StatusLabelStyle}" Text="{Binding ExecStatus}" Grid.Column="1" Grid.Row="1" />
            </Grid>
        </Border>

        <Label Content="Status:" FontSize="10" Grid.Row="1"/>

        <Border Background="{DynamicResource Dialog_PanelBackground}" Grid.Row="2" CornerRadius="4" Margin="2">
            <TextBlock Text="{Binding CustomStatus}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2"/>
        </Border>

    </Grid>
</Window>
