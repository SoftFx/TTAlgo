<Window x:Class="TickTrader.BotTerminal.BotStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:TickTrader.BotTerminal"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        Style="{DynamicResource Dialog_Style}"
        Title="{Binding DisplayName}" Height="300" Width="300">

    <Window.Resources>

        <Style x:Key="BotNameLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Shell_PrimaryForeground}"/>
        </Style>

        <Style x:Key="StatusLabelStyle" TargetType="TextBlock">
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Foreground" Value="{DynamicResource Shell_SecondaryForeground}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Starting...">
                    <Setter Property="Foreground" Value="Yellow"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Stopping...">
                    <Setter Property="Foreground" Value="Yellow"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ExecStatus}" Value="Running">
                    <Setter Property="Foreground" Value="LightGreen"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource Dialog_ControlBackground}" CornerRadius="4" Margin="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <l:FlatButton x:Name="StartStop" Style="{DynamicResource BotStateStartStopButtonStyle}" IsEnabled="{Binding CanStartStop}" Grid.RowSpan="2"/>
                <TextBlock Style="{StaticResource BotNameLabelStyle}" Text="{Binding BotName}" Grid.Column="1" Grid.Row="0" />
                <TextBlock Style="{StaticResource StatusLabelStyle}" Text="{Binding ExecStatus}" Grid.Column="1" Grid.Row="1" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label Content="Status:" FontSize="10" Grid.Column="0"/>
            <l:PopupButton Grid.Column="1"  
                           VerticalAlignment="Center" 
                           Margin="0 0 1 0"  
                           ToolTip="Settings"
                           Background="Transparent"
                           IsEnabled="{Binding HasParams}">
                <l:PopupButton.Content>
                    <Path Width="15" Height="15" Margin="4" Stretch="Fill" Fill="{Binding RelativeSource={RelativeSource AncestorType=l:PopupButton}, Path=Foreground}" Data="{DynamicResource Cogwheel}"></Path>
                </l:PopupButton.Content>
                <l:PopupButton.PopupContent>
                    <TextBox Text="{Binding Path=ParamsStr, Mode=OneWay}" 
                             BorderThickness="0"
                     HorizontalAlignment="Stretch" 
                     VerticalAlignment="Stretch" 
                     IsReadOnly="True"
                     TextWrapping="NoWrap" 
                     />
                </l:PopupButton.PopupContent>
            </l:PopupButton>
        </Grid>

        <Border Background="{DynamicResource Dialog_PanelBackground}" Grid.Row="3" CornerRadius="4" Margin="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="2 2 0 0"
                          Style="{DynamicResource DialogScrollViewer_Style}">
                <TextBox Text="{Binding CustomStatus}" 
                     HorizontalAlignment="Stretch" 
                     VerticalAlignment="Stretch" 
                     IsReadOnly="True"
                     TextWrapping="NoWrap" 
                     BorderThickness="0" />
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
