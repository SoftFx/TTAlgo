<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp">

    <ControlTemplate x:Key="BotsRepositoryView.Template.Classic">
        <Grid Background="{TemplateBinding Background}">
            <Grid.Resources>
                <Style TargetType="ListView">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>

                <Style x:Key="ToolButton" TargetType="Button">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>

                <Style x:Key="ReposItemStyle" TargetType="{x:Type ListViewItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>

            <TabControl>
                <TabItem Header="Bots">
                    <Grid Margin="10 5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold"/>
                        <ListView Grid.Row="1" ItemsSource="{Binding CurrentBots}" ItemContainerStyle="{StaticResource ReposItemStyle}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0 0 0 1" BorderBrush="LightGray" Margin="0 2">
                                        <Grid Margin="10 0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="14"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Version}" FontSize="14" HorizontalAlignment="Left"/>
                                            <TextBlock Grid.Column="2" Text="Update is available!" FontSize="14" FontWeight="Bold" HorizontalAlignment="Right" 
                                                       Visibility="{Binding HasNewVersion, Converter={StaticResource BoolToVis}}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </TabItem>
                <TabItem Header="Browse">
                    <TabItem.Content>
                        <ListView ItemsSource="{Binding Releases}" ItemContainerStyle="{StaticResource ReposItemStyle}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10 5">
                                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold"/>
                                        <ListView ItemsSource="{Binding Plugins}" ItemContainerStyle="{StaticResource ReposItemStyle}">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="10 0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="14"/>
                                                        <TextBlock Grid.Column="1" Text="{Binding Version}" FontSize="14"/>
                                                        <Button Grid.Column="2" Style="{StaticResource ToolButton}">
                                                            <Button.Content>
                                                                <fa:IconBlock Icon="Download"/>
                                                            </Button.Content>
                                                            <Button.ToolTip>
                                                                <TextBlock Text="Load package"/>
                                                            </Button.ToolTip>
                                                        </Button>
                                                    </Grid>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </TabItem.Content>
                </TabItem>
            </TabControl>


        </Grid>
    </ControlTemplate>

    <Style x:Key="BotsRepositoryViewStyle.Classic" TargetType="Window">
        <Setter Property="Template" Value="{StaticResource BotsRepositoryView.Template.Classic}"/>
        <Setter Property="Background" Value="{DynamicResource Dialog_Background}"/>
        <Setter Property="ResizeMode" Value="CanResize" />
        <Setter Property="Width" Value="700"/>
        <Setter Property="Height" Value="500"/>
        <Setter Property="MinWidth" Value="400"/>
        <Setter Property="MinHeight" Value="300"/>
        <Setter Property="ShowInTaskbar" Value="False"/>
    </Style>
</ResourceDictionary>