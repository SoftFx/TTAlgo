<Window x:Class="TickTrader.BotTerminal.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ad="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:l="clr-namespace:TickTrader.BotTerminal"
        Title="ShellView" Height="700" Width="900"
        MinWidth="500"
        MinHeight="500"
        Background="{DynamicResource Shell_BackgroundBrush}"
        SnapsToDevicePixels="True">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="250" MaxWidth="400"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="150"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Column="0" Grid.RowSpan="3" Style="{DynamicResource ShellTabControl_Style}" Margin="5,5,0,5">
            <TabItem Header="Symbols">
                <ContentControl x:Name="SymbolList" DockPanel.Dock="Left"/>
            </TabItem>
            <TabItem Header="Watchlist"/>
            <TabItem Header="ttBots"/>
        </TabControl>

        <GridSplitter Grid.Column="1" Grid.RowSpan="3" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Transparent"/>

        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!--<TextBlock Text="Graphs Should Be Here..." FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource Shell_PrimaryForeground}"/>-->
            <ComboBox 
                      ItemsSource="{Binding Source={x:Static l:ThemeSelector.Instance}, Path=ThemeNames}"
                      SelectedItem="{Binding Source={x:Static l:ThemeSelector.Instance}, Path=SelectedTheme}"/>
            <Button Name="Connect" Content="Connect"/>
            <Button Name="Disconnect" Content="Disconnect"/>
            <Button Name="Cancel" Content="Cancel"/>
        </StackPanel>

        <GridSplitter Grid.Column="2" Grid.Row="1" Height="5" Width="Auto" HorizontalAlignment="Stretch" Background="Transparent"/>

        <TabControl Grid.Column="2" Grid.Row="2" Style="{DynamicResource ShellTabControl_Style}" Margin="0,5,5,5">
            <TabItem Header="Positions">
            </TabItem>
            <TabItem Header="Orders"/>
            <TabItem Header="History"/>
            <TabItem Header="Journal"/>
        </TabControl>

    </Grid>
</Window>
